# ...existing code...
# Dev-only: let Hibernate create/update missing tables. Replace with migrations in production.
spring.jpa.hibernate.ddl-auto=update
# Optional: show SQL during startup
spring.jpa.show-sql=true
# ...existing code...

